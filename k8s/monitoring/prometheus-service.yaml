apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: cohort
  labels:
    app: prometheus
spec:
  selector:
    app: prometheus
  type: NodePort
  ports:
    - name: http
      port: 9090
      targetPort: 9090
      # Access: http://EC2_PUBLIC_IP:30909
      nodePort: 30909
